name: REL-001A deterministic materialization

on:
  push:
    branches: ["rel/001a-documentation-truth"]

permissions:
  contents: write

env:
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  materialize:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: ${{ github.sha }}
          persist-credentials: true
          fetch-depth: 0
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: "3.12.10"
      - name: Build intended final tree
        run: |
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          rm .github/workflows/adwf-rel001a-materialize.yml
          python .adwf/scripts/generate_pipeline.py --check
          python .adwf/scripts/docs_freshness.py --write --reviewed-at "2026-08-15T11:37:00Z" --valid-until "2026-11-15T11:37:00Z"
          python .adwf/scripts/generate_manifest.py
      - name: Validate intended final tree
        run: |
          python .adwf/scripts/validate_ci.py
          python .adwf/adwf.py provider-check --input .adwf/requests/github-public-standard.json
          python .adwf/scripts/validate_framework.py
          python .adwf/scripts/validate_pipeline_ir.py
          python .adwf/adwf.py doctor --scope package_integrity
          python .adwf/adwf.py doctor --scope config_health
          python .adwf/adwf.py self-test
          python .adwf/scripts/validate_docs.py
          python .adwf/scripts/generate_manifest.py --check
          git diff --check
      - name: Commit deterministic projections only
        run: |
          git restore --source="$GITHUB_SHA" -- .github/workflows/adwf-rel001a-materialize.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .adwf/docs-registry.json MANIFEST.json SHA256SUMS.txt
          git diff --cached --quiet && exit 0
          git commit -m "Refresh REL-001A deterministic projections"
          git push origin HEAD:rel/001a-documentation-truth

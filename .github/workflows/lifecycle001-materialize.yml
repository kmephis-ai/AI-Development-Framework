name: LIFECYCLE-001 transport staging

on:
  push:
    branches:
      - __never_trigger_lifecycle001_staging__

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo staging-disabled
